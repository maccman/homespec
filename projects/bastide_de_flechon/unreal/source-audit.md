# Frozen Blender source audit for the Unreal conversion

This records the read-only source/IR audit and the conversion owner's subsequent native evidence. Source reasoning, evaluated Blender proofs, native import checks and image review retain separate scopes. The source archives, `.blend` files and original images were not changed.

The authoritative packed model is `/Users/cloud/.codex/worktrees/49bf/homespec/projects/bastide_de_flechon/deliverables/model/house_walk.blend`, SHA-256 `caa9878ba4ca5d71850f4887e0ce3d00fd3f7f218fcafc5fc2873f83d392d23a`. The navigation extractor verified those bytes and the final IR SHA-256 `79f9c91081593254ecf7480057638a46e21913eff01f24a053fda54ade1c6e4b` before producing `navigation-data.json`. The final packed save contains 64 embedded image datablocks; 67 was the earlier pre-save count. Scene instructions and material recipes were read from the final source worktree, not substituted by the fresh worktree's version.

## Coordinate and navigation evidence

The IR generation `9340bb88d26d4244a5364682e8d81807` contains 326 entities, including 19 room spaces, 45 door/window/arch openings, two staircases and 55 architectural clash records. IR coordinates are **millimetres**; Blender geometry and bookmark positions are **metres**. L0 structural floor is 0; L1 is 3.3 m. The source main exterior rectangle is 8 × 11 m. Known dimension checks suitable for import include `D_MASTER_BATH`'s 1.1 × 2.15 m structural opening with 1.02 m clear width, `D_LAUNDRY`'s 0.9 m opening/0.82 m clearance, and D_FRONT's center at Blender (4.0, 0.175, 0) m. Store the selected Blender-to-Unreal transform, then verify it against actual imported bounds. Multiply metres by 100 for Unreal centimetres; do not blindly multiply native millimetres by 100.

The 26 bookmarks are **camera eye poses**, not grounded player capsule centers. Bookmarks 1 (overview) is at z=4 m; 23 (laundry) and 24 (WC) are at z≈1.06 m and 1.12 m. Preserve these exact camera poses for comparisons, and derive separate safe walk spawns using a floor trace and standing capsule sweep. All 26 original poses and exposure values remain in the navigation JSON, each explicitly marked unverified for grounded spawning. Some bookmarks are detail or duplicate room views, so 26 bookmarks does not mean 26 distinct enclosed rooms.

The salon tile tops and principal floorboard tops are 3 mm above structural floor (`salon_envelope.tiled_floor`, `principal_envelope.floorboards`). Use the final evaluated finish for foot contact. Do not replace `F1_MAIN` or `F1_H` with a bounding box collider: their actual meshes contain the stair cuts; `H_GALLERY_VOID` is an intentional open gallery. `THRESHOLD_*` slabs bridge paired openings across independently modeled adjacent walls and must remain.

The candidate graph in `navigation-data.json` contains 29 possible connections. It is a list to test, not a certification that closed doors or furniture allow passage:

| Connection | Exact source entities |
|---|---|
| Living ↔ dining | Open shared boundary at y=7.3 m |
| Dining ↔ kitchen | `A_KITCHEN`, `A_DINING_K`, `THRESHOLD_K_DINING` |
| Kitchen ↔ hall | `A_HALL_K`, `A_K_HALL`, `THRESHOLD_HK_L0` |
| Hall ↔ guest corridor | `A_HALL_GUEST`, `A_GUEST_HALL`, `THRESHOLD_HA_L0` |
| Hall ↔ upper landing | `ST_HALL` |
| Dining ↔ principal bathroom | `ST_MASTER` spiral; upper exit inside bathroom zone |
| Principal bedroom ↔ bathroom | `D_MASTER_BATH` |
| Principal bathroom ↔ bedroom above kitchen | `A_MASTER_LINK`, `A_DRESSING_K`, `THRESHOLD_K_MASTER` |
| Bedroom above kitchen ↔ its bathroom | `D_BATH3` |
| Upper bathroom ↔ landing | `A_BED3_HALL`, `A_HALL_BED3`, `THRESHOLD_HK_L1` |
| Landing ↔ upper guest suite | `A_HALL_SUITE4`, `A_SUITE4_HALL`, `THRESHOLD_HA_L1` |
| Upper guest suite ↔ bathroom | `D_BATH4` |
| Ground guest corridor ↔ bedrooms | `D_BED1`, `D_BED2` |
| Ground bedrooms ↔ bathrooms | `D_BATH1`, `D_BATH2` |
| Guest corridor ↔ service rooms | `D_LAUNDRY`, `D_WC` |

Both stairs rise 3.3 m in 20 × 165 mm risers. `ST_HALL` has 1.0 m reported clear width, 260 mm straight going, about 272 mm winder walkline going; its full tread polygons, walkline and foot/upper approach zones are in the JSON. `ST_MASTER` is a 450° spiral centered at (6.5, 9.45) m, outer radius 1.03 m, inner radius 0.1 m, clear width 0.93 m, walkline going about 283 mm. Its reported turn headroom is 2.585 m; both stair source checks report 2.0 m physical headroom with no listed obstruction. These source checks do not prove native capsule clearance at every landing. The earlier native v2/v3 tests used a 0.30 m radius and 0.88 m half-height. The current test character uses a 0.28 m radius (0.56 m diameter), retaining the 0.88 m half-height, and requires fresh native evidence. Test both ascending and descending, especially winding inner edges, landing turns and approach furniture. Preserve tread collision or build an explicit tread-following proxy; a whole-stair convex hull closes the stairwell and underside.

## Door states, glazing and collision exceptions

`BastideGableDoor` already opens the two **D_FRONT lower leaves 78° inward**. It combines open lower frame members with fixed upper frame members in `D_FRONT`, and open lower panes with fixed upper panes in `D_FRONT.glass`. Preserve that geometry and its polygon locations. The lower passage ends at z=2.780 m; fixed glass above the lower transom and the master bedroom's semicircular fanlight remain barriers. Never infer “all glass under this door ID is open”, or rotate the complete opening.

`D_ENTRY` is a 2.5 m wide envelope with a 1.27 m clear **central** passage and a 2.655 m clear height. `D_ENTRY.leaf` contains two closed central opaque leaf solids; `D_ENTRY.glass` contains fixed sidelights and transom. `N_HALL`/`N_HALL.glass` is the fixed upper light starting at z=3.55 m. If opening the entrance in the conversion, split central leaves and include their attached `exterior_entry_leaf_0_*`, `exterior_entry_leaf_1_*`, `exterior_entry_handle_plate_{0,1}`, `exterior_entry_handle_{0,1}` details in the corresponding hinge groups. Do not remove the sidelights or upper light to manufacture a route.

The ordinary interior doors have separate `.leaf` entities: `D_MASTER_BATH`, `D_BED1`, `D_BED2`, `D_BATH1`, `D_BATH2`, `D_BATH3`, `D_BATH4`, `D_LAUNDRY`, `D_WC`. Source construction fills them in the closed plane and source presentation does not call `scene.hide` on these leaves. The final Blender inventory must confirm their evaluated state. Rotate leaf geometry into a consistent open state or make it operable; disabling collision on a visually closed door is not physical walking. Retain 0.82 m clearances on the smallest bath/service doors and report a tight approach if the selected capsule cannot turn.

The other exterior glazed door candidates are `D_E1`, `D_E2`, `D_W1`, `D_W2`, `D_PERGOLA`, `D_KITCHEN_GARDEN`, `D_KITCHEN_TERRACE`, `N_GUEST_E0`, `N_GUEST_E1`. Frame/grille and pane parts may share one entity mesh: determine movable lower portions from evaluated geometry before opening them. The kitchen terrace nominal 1.9 m envelope has only 0.8825 m reported clearance per side of its center mullion; do not treat its full width as an unobstructed aperture. **N_GUEST_E0 and N_GUEST_E1 are doors despite their N_ prefix**. Conversely `N_HALL` is fixed window glazing although it uses an arched-door superclass.

`N_E1`, `N_E2`, `N_W1`, `N_W2`, `N_MASTER_N`, `N_SUITE4_E0`, `N_SUITE4_E1`, `N_GUEST_W`, `N_BATH4_W`, `N_HALL`, `N_BED3_S`, `N_BATH3_E` and their `.glass` entities are fixed view openings. Keep physical glazing barriers. `FP_HEARTH` is a fireplace cavity, **not a route**. Static triangle collision preserves arch/window voids; a convex hull around a wall or surround fills them. Detail meshes can use no independent collision only when the underlying architectural collider correctly represents the physical wall/step and its openings. Bed/furniture bounds and railings require blockers; thin cords, foliage leaves and small surface trim need not each have a hull.

The exact three existing guest-door route reports name `guest_1_queen_upholstered_base`, `guest_1_queen_white_duvet`, and `guest_1_queen_woven_coverlet` in front of `N_GUEST_E1`, near (-1.7 to -1.9, 27.3 to 27.8) m. Preserve the source bedroom layout; test the interior `D_BED1` route separately. This exterior door must not be reported clear merely because its structural opening width passes a rule.

Source visibility is meaningful. Old D_FRONT surround, louvred shutters, generic pergola posts/ribs/bars and selected base roof trim are hidden because detailed replacements are present. Hidden Boolean cutters also exist. Export evaluated visible geometry plus required dependencies; do not display every source mesh regardless of visibility. This is a scene-wide replacement policy, not per-camera hiding.

## Material translation requirements

The final shaders cannot be represented by copying each material's first image and Principled defaults. Keep original image bytes/hashes and node/slot/mapping inventory, and bake unsupported expressions on copies of the final evaluated meshes. Preserve each rendering UV layer: a separate editing `UVMap` can contain primitive coordinates. Assign stable target names by source image/material/object identity, not incidental export order.

| Family and source modules | Actual source behavior | Conversion requirement |
|---|---|---|
| Shared walls, `material_details`, `fidelity_materials` | Ground hall ochre selected by world z<3.25 m and inward normal dot>0.7. Main upper clay selected by z>3.28 m and inward normal. Shared stone exterior retained. | Bake the evaluated mask into target material sets or split per-face/height regions; one copied image cannot preserve both exterior and interior finishes. |
| Salon, `salon_materials` + `homespec/blender/material_assets` | Generated pigment with tint/saturation, UV or object mapping, optional per-object random color phase. Independent metric world roughness and two bump scales; woven fibers are separate. | Bake base color without illumination, independent roughness and normals. Preserve random phase on each instance or store it as an instance parameter. |
| Salon floor/rug | Floor images use normalized individual stone-face UVs; rug uses one complete [0,1] rectangle, long direction V. Physical 2.5 mm joints are geometry. | Preserve these UVs and borders; do not tile the whole rug or merge joints away. |
| Principal suite, `principal_materials` | Physical/member UV pigment, antique walnut rotated 90° because its image fibers run V. Independent world finish noise. Wood relief aligned with member U. Some `old_oak` material is masked by world y<6.94 m to retain bathroom continuation. | Preserve member UV orientation, separate endgrain slots and room mask. Bake the evaluated masked response or reproduce the exact region split. |
| Principal board floor | 120 separate boards, rendering UVs in metres, source finish top z=3.303 m. | Retain metric grain scale and source material slots; prioritize the render-active layer, not first UV by name. |
| General fabrics, `fidelity_materials` | Object metre box pigment except UV coverlets and specified cloth; normalized coverlets preserve the printed border. Independent high-frequency weave/pores and finish noise. | Preserve per-object mapping before batching. Do not derive roughness or height from dark paisley/ikat printing. |
| Exterior, `exterior_materials` | World Position metre box pigment (blend .16) or shutter board UV; per-object random color phase. Independent world finish noise and shallow relief. UV U follows horizontal shutter-board grain. | Bake object-specific phase before joining or store phase. Preserve board grain scale. Geometry supplies actual rubble joints, stone courses and tile silhouettes. |
| Endgrain and legacy procedural finishes | Generated coordinates/noise/wave/ramp networks, sometimes per-axis long grain; cross-section face material slots. | Bake these unsupported color/roughness/normal expressions; a flat material fallback loses visible joinery. |
| Glazing/metals | Clear glass transmission=1, typical salon IOR=1.48 and roughness .015–.025. Independent metallic response on iron, patinated bronze, brass and zinc. | Dedicated glass material and correct sidedness/reflections. Transfer metallic directly, no specular/glossiness substitution. |
| Curtains/shades | Salon curtain mixes .48 diffuse translucency then .13 transparent open weave. Principal curtain has .075 diffuse translucency. Linen lamp shade mixes .55 diffuse translucency with a 0.55 mm solidified shell. | Dedicated thin-fabric transmission approximation; opacity alone cannot reproduce backscatter. Record remaining mismatch against Cycles. |
| Foliage | Linked leaf geometry/instances, alpha/translucency where actually present; unmeasured planting. | Preserve instance transforms, two-sided leaf response and actual alpha semantics. Inspect final graph rather than assigning transparent leaves indiscriminately. |

Baking recommendations: use the frozen model in an unsaved working copy, evaluate modifiers/instances and retain original object coordinates or bake before batching. Add a separate non-overlapping bake UV channel without changing the source render UV used by existing Texture Coordinate/UV Map nodes. Bake base-color shader expressions through emission (or diffuse COLOR-only for compatible opaque graphs), roughness/metallic/opacity separately, and final surface normal into tangent space. Mixed surfaces require explicit masks or expression evaluation; taking the first Principled node is insufficient. Keep baked base color in sRGB; roughness, metallic, opacity and normal are linear/non-color. Blender tangent normal convention and Unreal convention require a documented green-channel choice verified on a directional relief sample. Never include light, shadow or AO in base color. Do not infer relief from generated pigment luminance. The image manifests explicitly state these are photo-informed generated colors, not measured reflectance or displacement scans.

Use visible surface area and camera distance to choose baking density and texture grouping; walls/hero furniture require more than tiny decorative details. Record chosen pixels/metre and total memory instead of claiming an unmeasured density. Separate reusable master materials for opaque PBR, glass and thin fabric/foliage are preferable to thousands of unique shader graphs. Each material instance must retain the source-to-target channel mapping, tint/bake status, UV selection and any documented approximation.

## Lighting and retained source limitations

The coherent `walk` preset is one inferred low model-southwest sun: ray direction `(0.72, 0.62, -0.31)`, Blender energy 2.6, source angular size .80°, color `(1, .91, .77)`; a sun-free sky rotates 115° with strength 1.65. These Blender intensities are not Unreal lux/lumens. The walkthrough has a reduced **20% aperture-light approximation**, neutral `(1, .96, .90)` supplemental light color, and practicals on. Fireplace flame/light fixtures are off in the walk. Preserve this intent rather than turning on the salon58 photo/fire preset by default. The packed scene has three Eevee probes; they do not transfer to Unreal GI.

Interior bookmark exposure adds 1.25 stops except ground bathrooms, laundry, WC and corridor; exteriors have no offset. Rebuild continuous eye adaptation for walking, and reserve exact per-bookmark source exposure for labeled comparison captures. Source camera tone mapping/white balance and Unreal tone mapping are different. Match the final local PNG masters at actual camera positions and inspect windows, shadow leaks, orange fill and highlight clipping across garden-to-room transitions. `fidelity_lighting.area` suppresses specular on supplemental aperture lights; transferring them as ordinary specular rectangle lights changes reflections.

The completed image delivery is not a collision-free house. Retained limits from the frozen `final-delivery.md`: bed3 glazing .086 against .1 guideline; 55 native architectural clashes; 46 dressed raw findings (43 inside-wall, three guest-route findings), which are separate overlapping scopes; rubble/trim overlap in shutter/sill detail; irregular inner-left bathroom arch boundary and facets in view 18; inferred roof/oculus heights, courtyard terrace levels, door states, furnishings and simplified planting. `exterior_setting.py` explicitly adds no courtyard risers because `T_PERGOLA` and the house share zero level, despite four photographed risers. Do not add isolated invisible ramps or steps without disclosing the altered conversion geometry.

Reproduce the navigation evidence from this worktree:

```sh
python3 projects/bastide_de_flechon/unreal/tools/build_navigation_data.py \
  --source-root /Users/cloud/.codex/worktrees/49bf/homespec \
  --output projects/bastide_de_flechon/unreal/navigation-data.json
```

The extractor has no Blender/Unreal dependency, rejects a different packed source or IR hash, and writes only the selected new output. Its 19 rooms, 45 opening records, two stairs, 29 candidate routes and 26 bookmark records were generated successfully. Native import, grounded spawning, movement, collision and visual equivalence remain the conversion owner's separate verification work.

## Conversion scope and validation receipts

The final source's `Kitchen · photo00 flowers (alternate)` collection is hidden for rendering even though its 233 member objects do not individually set `hide_render`. Render scope must follow collection ancestors and excluded layer-collection paths, across enabled render view layers. A second visible collection link can keep an object visible. The shared `render_visibility.py` helper now implements those rules, and the independent source inventory records each object's effective visibility and collection paths. Viewport-only hiding does not replace render visibility. The refreshed native inventory confirms **15,709** visible mesh/curve/surface/font objects, removing the 233 alternate objects and restoring the three water surfaces below. Full export coverage remains a separate final receipt check.

`FOUNTAIN.water`, `POOL.water`, and `RILL.water` use `pool_water`, whose active material output contains both a Principled surface and volumetric absorption. These are retained as water surfaces with no collision. Their volume absorption is omitted from the surface-channel bake and recorded as an approximation; the importer uses a dedicated tinted water material. They are not volume-only flame objects and must not be excluded or described as fire replacements. The source walk preset's fire remains off. Only objects whose active outputs contain volume with no surface qualify for the volume-only exclusion.

Source entity meshes with uppercase native IDs remain separate export groups in the house and near exterior, including walls, floors, thresholds, stairs, doors, and beams. Smaller decorative objects retain spatial batching. Export receipts retain exact evaluated world-vertex bounds for each retained surface partition before decimation, after the disclosed bevel simplification and static door opening. Those bounds avoid the understated curve bevel extents in Blender's object bounding boxes. They do not by themselves prove the final Unreal transform: the importer must compare actual native bounds as well.

The bake configuration records all script/helper/navigation hashes, atlas limits, channel semantics, normal convention, and the deterministic replacement for source per-object random phase. The latter uses a name-derived phase; it preserves stable variation, but does not preserve the original Cycles random value. Thin-fabric diffuse transmission, water absorption/refraction, and lighting remain documented native approximations. Base color, roughness, metallic, opacity and optional linear emission are separate evaluated shader expressions. Atlas receipts record resolution, surface area, UV occupancy, and effective pixels per metre; a capped atlas is not a claim that the requested density was achieved.

The pure-Python export validator checks the frozen source bytes, independently inventoried visible-object coverage, all packed image identities, 26 bookmark poses, source UV/material records, configuration and receipt hashes, FBX presence, channel hashes and PNG integrity, bounds, and density metadata. It reports partial exports explicitly and does not certify gameplay, visual equivalence, or packaging. A full native import should be preceded by:

```sh
python3 projects/bastide_de_flechon/unreal/tools/validate_export.py \
  --export-root out/unreal/export \
  --inventory out/unreal/audit/scene-inventory.json \
  --navigation projects/bastide_de_flechon/unreal/navigation-data.json \
  --manifest out/unreal/export/manifest.json \
  --wire-replay out/unreal/audit/wire-preservation-replay.json \
  --prior-manifest out/unreal/audit/export-before-resume.json \
  --prior-manifest out/unreal/export-near/manifest.json \
  --prior-manifest out/unreal/audit/export-west-before-resume.json \
  --prior-manifest out/unreal/audit/export-before-uv-resume.json \
  --prior-manifest out/unreal/audit/export-before-final-repairs.json \
  --topology-repair out/unreal/audit/topology-production-first-failed.json \
  --topology-repair out/unreal/audit/topology-production-v2.json \
  --topology-repair out/unreal/audit/preserve-roof.json \
  --topology-repair out/unreal/audit/preserve-sheet.json \
  --duplicate-face-proof out/unreal/audit/duplicate-face-proof.json \
  --empty-source-proof out/unreal/audit/empty-source-proof.json \
  --foliage-review out/unreal/audit/near-foliage-review.json \
  --output out/unreal/audit/export-validation.json \
  --require-complete
```

The importer can be used separately for intentionally partial pilots and conditionally verifies asset hashes present in its input. It also requires the frozen source hash and a complete manifest for an unrestricted import. The strict validator remains the independent full-export integrity gate; an importer completion receipt alone is not evidence of full frozen-source coverage. Targeted unit tests cover collection visibility, missing/corrupted assets, forged completion, changed source bytes, unsupported exclusions, water/glass semantics, UV loss, changed coordinates, stale receipts, exact bounds unions, source wire preservation, corrected replay identity/geometry, sparse versus collapsed UV density, narrow reduction exceptions, cached provenance and working-copy topology repairs. Additional mutations verify that an archived failed run can prove its finished receipts, but cannot excuse unfinished repairs, removed referenced UV maps, changed retained UV coordinates, or altered geometry. Native Blender/Unreal tests are the conversion owner's separate evidence.

The first deterministic kitchen replay established that collapse decimation can move vertices beyond the original bounds. Two pendant chunks matched their reduction replay exactly, and Blender's FBX reimport matched their receipt bounds within 1.91 micrometres. Their coiled weft and crossed support wires had expanded by up to 47.3 mm on a roughly 471 mm diameter shade. The corrected exporter exempts exactly `kitchen_fine_wire_pendant_{0,1,2}_{coiled_wire_weft,supporting_crossed_wire}` from collapse reduction, retaining all 1,047,168 evaluated source triangles in those six parts. The config and per-object policy record that exemption; the validator checks unchanged triangle counts against the independent inventory. The conversion owner's corrected native Blender replay in `out/unreal/audit/wire-preservation-replay.json` confirms both chunks now have **zero expansion**, identical replay/receipt bounds and source-part counts, and at most 1.91 micrometres FBX reimport error. The optional `--wire-replay` input binds that evidence to the exact final FBX, receipt and bake configuration; it rejects stale evidence or changed geometry even if its summary booleans claim success. The diagnostic's `decimation_origin_proven=false` is expected for the corrected case because no expansion remains to attribute.

Small silhouette movement from explicitly recorded distant foliage reductions is reported as a fidelity approximation. It does not establish an axis or scale error. Aggregate source bounds must still equal the union of recorded exact source-part bounds, and the importer must still compare actual post-FBX bounds against exported metre bounds using the declared coordinate mapping. The numerical bound tolerance is a 0.1 mm floor or eight float32 ULPs at the scene magnitude, whichever is larger. Sparse thin-wire UV islands are not automatically a failed bake: the corrected pendant atlases occupy roughly 0.9% while providing 156 and 213 pixels per metre. Low occupancy is an efficiency warning when density is adequate; a density below one pixel per metre, or low occupancy combined with density below half the target, remains an error.

The first distant-foliage FBX contained 305,144 control points, including 270,264 loose points with no polygon references. Five extrema belonged entirely to those loose points. Its 16,800 triangles were all nonzero area, and polygon-supported bounds matched the actual Unreal bounds within 2.14 micrometres; the 23.99 cm apparent shrinkage came from comparing unused control points with rendered geometry. `foliage-fbx-support.json` records the diagnosis. Import validation therefore retains all-control-point provenance and separately compares polygon-supported geometry to native bounds, without increasing the axis/scale tolerance.

The conversion owner accepted the measured 1.269 mm upward reduction movement of `MASTER_TRUSS_BRACES`, a roughly 7.3 m wide assembly, as a narrow fidelity approximation. The validator exception is restricted to FBX SHA-256 `906a6e9729d279fbb87008281c498530dfd3961c98ce9d30baede3e77da614da`, its exact bake configuration, and at most 2 mm growth. Larger deviations or different artifacts remain errors. The original bounds and measured movement remain in the report, and the native roof requires a visual check.

After 256 chunks, the positive-house group `solid_opaque_house_x0_y3_z0` twice stalled inside Blender's vertex-normal traversal when entering Edit Mode. A bounded wrapper experiment found two polygons with repeated vertex indices in the joined working mesh. `mesh.validate(verbose=True, clean_customdata=False)` removed exactly those two polygons and four triangulation entries in about 16 ms; all 89,116 vertex positions retained the identical hash and both all-vertex and polygon-supported bounds were unchanged. Edit Mode and the isolated export then completed. The original source and exporter were unchanged. The repaired chunk includes `geometry_preparation` with wrapper identity, policy, topology/position hashes and before/after counts; `--topology-repair` binds this to the completed wrapper report and exact final receipt. This is a disclosed working-copy topology repair and still requires final visual review.

Resuming the unchanged exporter reuses matching chunk receipts but does not reproduce per-object reduction records for cached chunks. Repeated `--prior-manifest` inputs preserve that evidence transparently: the validator requires the same frozen source/configuration and an identical current chunk receipt before inheriting any object's reduction record. It records archive/shard manifest hashes, matched chunks and inherited object names, rejects conflicts, and ignores unmatched prior objects. The archived pre-resume manifest, separately completed near-shard manifest and partial west-shard snapshot are explicit inputs above. Additional merged shards require their own final manifest inputs. Only manifests whose exact receipts entered the final cache should be supplied. The 256-chunk primary snapshot, 121-chunk near shard and 90-chunk west snapshot are not claims that the complete export succeeded; final coverage and native import checks remain required.

The production wrapper supports `--limit 0 --resume --allow-primary` with empty `--only`. It validates each new joined working mesh, intercepts the unchanged exporter's JSON serialization, and adds `geometry_preparation` to a repaired row before its first receipt write. The same row then enters the streamed manifest. Published receipts and cached rows are never rewritten by the wrapper. Its report hashes only prepared receipts generated by that run. The exact original default for pixels per metre is preserved by omitting the CLI argument at 192; explicitly parsing `192.0` changes the canonical configuration hash even though Python numeric equality considers it equal. A rejected isolated configuration remains separate evidence and must not be merged or used to supply provenance for the production export.

The first production wrapper run stopped after 294 published chunks when the joined group `solid_opaque_house_x1_y1_z0` exhausted Blender's eight UV-map slots: seven distinct source map names plus `_SourceUV` left no slot for `UnrealAtlas`. Its unchanged partial manifest and failed report are archived as `export-before-uv-resume.json` and `topology-production-first-failed.json`; the latter retains the exact completed receipt evidence for two topology repairs. The next wrapper version reserves an atlas slot after joining, pruning only shader-unreferenced maps. It recursively checks material group trees and named UV/Attribute references, always retains `_SourceUV`, and verifies every retained UV array hash and geometry snapshot is unchanged. Each affected row receives `uv_preparation` before receipt publication. The completed continuation wrote the separate `topology-production-v2.json` report and published all 669 planned chunks. Its 164 newly prepared meshes include eight topology repairs and one UV-layer pruning event; reused chunks retain their earlier receipt evidence. Validation binds both report versions to exact final receipt bytes and finished events, preserving the first run's failure rather than treating it as a completed export.

The archived 294-chunk state separately passed 63,352 validation checks with zero errors and an explicit partial status. The resumed wrapper also removed two overlapping duplicate faces from `MAIN_BEAM2`, reducing 6,445 polygons to 6,443 and 18,540 triangulation entries to 18,537. Its 9,275 vertex positions, position hash, and both bounds were unchanged. The conversion owner's isolated `verify_duplicate_faces.py` replay in `duplicate-face-proof.json` matched the exact published receipt's before/after topology hashes, proved identical unique polygon cycles (including cyclic edges, rather than only sorted vertex sets), and verified the surviving material/UV assignment existed on a prior face. The validator accepts this general surface-support proof only when tied to the exact source/exporter/configuration/FBX/receipt, with exact removed-face/loop/triangle counts; it does not permit arbitrary polygon removal. Native beam material, normal and roof appearance still require visual review.

The first final audit correctly left the 669-chunk export incomplete: three visible source objects had produced no mesh geometry, and further source-fidelity findings required resolution. `inspect_empty_sources.py` independently confirmed zero vertices, edges, loops, polygons and triangles under both the frozen source evaluation and render-enabled modifier settings for `salon_envelope_D_W1_arched_glazing_beads`, `salon_envelope_D_W2_arched_glazing_beads`, and `salon_fp_mantel_profile_6`. The first two are empty curves; the mantel's evaluated Boolean result is empty. `empty-source-proof.json` binds this to the unchanged source and native inventory. These objects are explicitly counted as native empty geometry, separately from rendered surface objects. They were not silently dropped from source accounting.

The n-gon repair in `SM_solid_opaque_house_xn2_y4_z0_000` removed two repeated-vertex faces containing nine loops, hence five triangulation entries: `9 − 2 × 2 = 5`. Its vertex positions and supported bounds remained unchanged. The validator now checks this exact loop/face triangulation identity; the earlier assumption that each removed face contributed at most two triangles was incorrect.

Two source objects received isolated preservation bakes after source-bound growth was identified: `MASTER_ROOF_TIMBERS` and `guest_1_queen_turned_sheet`. The wrapper removed only their unapplied DECIMATE modifiers and recorded identical before/after native geometry snapshots. The replacement roof chunk contains 2,669,270 triangles and the sheet-containing chunk 81,508, with zero growth beyond their recorded source bounds. Their receipt-level `geometry_preservation` and separate `preserve-roof.json`/`preserve-sheet.json` reports retain source/configuration identity and exact file hashes. The finalizer replaces only these two rows, preserving the prior FBX, textures and receipts in `out/unreal/audit/replaced-assets`; old reductions are not inherited into the replacements.

Twenty-one near-foliage deviations are reviewed approximations under the pinned `near-foliage-review.json`, not a blanket rule for exterior chunks. Every reduced member is independently tagged as a source plant. The mixed chair/vegetation chunk additionally preserves 18 independently verified chair components (396 supported vertices, 720 triangles) with no cross-region triangles and at most 1.91 micrometres per-component bounding-box error. `foliage_approximation.py` rechecks the exact FBX/configuration/inventory/reduction evidence and reconstructs those chair components before allowing a foliage warning. Native transform tolerances remain unchanged, and silhouette/lighting appearance still requires visual inspection.

`finalize_export.py` validated the candidate before atomically publishing the final manifest. It backs up and verifies original replacement assets before any swap, restores them if validation fails, and retains the immutable `export-before-final-repairs.json`. The final export passed **117,185 checks with zero errors and 339 disclosed warnings**: **669 chunks**, **15,706 surface-bearing source objects**, and **3 native empty objects**, accounting for all **15,709 visible source geometry objects**. Final manifest SHA-256: `797ee92b4d4a6cd8503e7e6dff85302d884c3f568e6016b93c886be08570aea4`. The warnings retain density shortfalls, sparse packing and disclosed geometry approximations; this artifact audit does not certify native rendering, collision, navigation or packaging.

The complete Unreal import subsequently succeeded in `out/unreal/runs/20260906T225724Z_2febeef9_import_editor`: its native receipt is complete and the independent stage receipt records process exit 0. All 669 native mesh bounds passed, with maximum recorded error 0.00214386 cm against polygon-supported FBX bounds. The run rechecked and reused 657 persisted meshes from the preceding disk-exhausted run, then imported the remaining 12. That earlier failed run was not reclassified as successful. The completed native inventory contains 669 material instances and 1,861 textures.

The first complete camera/runtime audit, `20260906T230901Z_5f5d9c46_validate_editor`, remains failed: its JSON was UTF-16 and screenshots were clamped to 1333×833 instead of the requested dimensions. Direct inspection of all final source/native images found excessive interior/exterior light contrast, blue or milky glass, degraded mirror reflections and a WC leaf opened across the narrow guest corridor. The independent views 13–26 comparison is retained in `out/unreal/audit/native-visual-review-13-26-v1.md`, including all source/native image hashes. Roof silhouettes correspond, but darkness prevents acceptance of close timber material detail. Source arch facets, reflected stone facets and toilet seams were distinguished from additional native artifacts.

The diagnostic runtime file contains 26 clear local walking targets and 59 actual route results, of which 19 passed and 40 failed. These raw findings are not an accepted validation stage or proof of connected access. New validation defaults to source-aspect 1280×800 captures and UTF-8 JSON. Native appearance and route corrections require fresh receipts and image review; the packaged app has not been built.

`inspect_render_properties.py` provides a separate read-only native source probe for light softness, area dimensions, sun angle and material backface flags at frames 1, 385 and 1249. It records source/script hashes and Blender identity and restores the original frame without saving the scene. The appearance pass consumes this additional evidence rather than modifying the frozen inventory or export hashes. Its settings and native receipt remain distinct from the final geometry artifact audit.
